// Basic smoke test for the WhatsApp clone app.
//
// This test verifies that the app widget can be instantiated without errors.
// Full widget tests for individual screens are co-located with their source
// files under lib/features/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app compiles and tests run', () {
    // This test exists to confirm the test runner is working.
    // Feature-specific tests live alongside their source files.
    expect(1 + 1, equals(2));
  });
}
